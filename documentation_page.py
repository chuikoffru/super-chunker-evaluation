"""
Страница документации и руководства пользователя
"""

import streamlit as st

def show_documentation_page():
    """Отображает страницу документации"""
    
    st.title("📚 Документация SuperChunker")
    st.markdown("### Полное руководство по использованию системы чанкования текстов")

    # Боковая панель с навигацией по разделам
    st.sidebar.header("📑 Разделы")
    section = st.sidebar.radio(
        "Выберите раздел:",
        [
            "🚀 Быстрый старт",
            "📊 Основные методы",
            "🧠 Продвинутые методы", 
            "⚙️ Настройки",
            "📈 Анализ результатов",
            "🔧 API документация"
        ]
    )

    if section == "🚀 Быстрый старт":
        st.header("🚀 Быстрый старт")
        
        st.markdown("""
        ## Добро пожаловать в SuperChunker!
        
        SuperChunker - это комплексная система для тестирования и сравнения различных методов разбиения текстов на чанки (chunking). 
        
        ### 🏗️ Структура приложения
        
        Приложение состоит из нескольких страниц:
        
        1. **🏠 Базовое чанкование** - основной интерфейс для быстрого тестирования методов чанкования
        2. **🧠 Продвинутый анализ** - тестирование семантических методов с PSR оценкой
        3. **📚 Документация** - эта страница с руководством
        
        ### ⚡ Быстрое начало работы
        
        1. **Выберите метод чанкования** в боковой панели
        2. **Настройте параметры** (размер чанка, перекрытие и т.д.)
        3. **Введите или загрузите текст** для анализа
        4. **Запустите процесс** и изучите результаты
        
        ### 📋 Поддерживаемые форматы
        
        - Обычный текст
        - Файлы .txt
        - Готовые примеры текстов разных типов
        """)
        
        st.info("💡 **Совет:** Начните с готовых примеров текстов, чтобы понять различия между методами")

    elif section == "📊 Основные методы":
        st.header("📊 Основные методы чанкования")
        
        st.markdown("""
        ## Доступные методы
        
        SuperChunker поддерживает четыре основных метода чанкования:
        """)
        
        # Character Chunker
        st.subheader("1. 📝 Character Chunker")
        st.markdown("""
        **Описание:** Разбивает текст на основе количества символов.
        
        **Особенности:**
        - Простой и быстрый
        - Предсказуемый размер чанков
        - Может разрывать слова и предложения
        
        **Параметры:**
        - `chunk_size`: размер чанка в символах
        - `chunk_overlap`: количество перекрывающихся символов
        
        **Когда использовать:** Для быстрой обработки больших объемов текста, когда точность границ не критична.
        """)
        
        # Recursive Chunker  
        st.subheader("2. 🔄 Recursive Chunker")
        st.markdown("""
        **Описание:** Интеллектуальное разбиение с приоритизацией разделителей.
        
        **Особенности:**
        - Сохраняет структуру текста
        - Приоритет: абзацы → предложения → слова → символы
        - Адаптивный размер чанков
        
        **Параметры:**
        - `chunk_size`: целевой размер чанка
        - `chunk_overlap`: размер перекрытия
        - `separators`: список разделителей по приоритету
        
        **Когда использовать:** Для текстов со структурой, когда важно сохранить смысловые границы.
        """)
        
        # Token Chunker
        st.subheader("3. 🪙 Token Chunker")
        st.markdown("""
        **Описание:** Разбиение на основе количества токенов (используется токенизатор OpenAI).
        
        **Особенности:**
        - Точный контроль количества токенов
        - Совместим с ограничениями LLM
        - Учитывает особенности токенизации
        
        **Параметры:**
        - `chunk_size`: количество токенов в чанке
        - `chunk_overlap`: перекрытие в токенах
        - `model_name`: модель для токенизации
        
        **Когда использовать:** При подготовке текста для языковых моделей с ограничениями по токенам.
        """)
        
        # Semantic Chunker
        st.subheader("4. 🧠 Semantic Chunker")
        st.markdown("""
        **Описание:** Семантическое разбиение на основе схожести предложений.
        
        **Особенности:**
        - Сохраняет смысловую целостность
        - Использует векторные представления
        - Адаптивные границы чанков
        
        **Параметры:**
        - `similarity_threshold`: порог схожести (0-1)
        - `max_chunk_size`: максимальный размер чанка
        - `api_key`: ключ OpenAI API (опционально)
        
        **Когда использовать:** Для текстов, где важна семантическая связность содержимого.
        """)

    elif section == "🧠 Продвинутые методы":
        st.header("🧠 Продвинутые семантические методы")
        
        st.markdown("""
        ## PSR Анализ
        
        PSR (Performance, Scalability, Reliability) - это комплексная система оценки качества чанкования.
        """)
        
        st.subheader("📈 Cumulative (Кумулятивный)")
        st.markdown("""
        **Принцип работы:**
        - Накапливает семантический контекст
        - Каждое предложение сравнивается с усредненным вектором всего чанка
        - Создает более связные семантические блоки
        
        **Преимущества:**
        - Лучшая семантическая связность
        - Подходит для связных текстов
        - Устойчив к локальным вариациям
        
        **Недостатки:**
        - Более медленный
        - Может создавать слишком большие чанки
        """)
        
        st.subheader("🏗️ Hierarchical (Иерархический)")
        st.markdown("""
        **Принцип работы:**
        - Многоуровневое чанкование по структуре документа
        - Разбивка по абзацам → семантическое чанкование → объединение
        - Учитывает иерархию документа
        
        **Преимущества:**
        - Сохраняет структуру документа
        - Хорошо работает с форматированным текстом
        - Балансирует размер и смысл
        
        **Недостатки:**
        - Требует структурированный текст
        - Сложность настройки
        """)
        
        st.subheader("🎯 Adaptive (Адаптивный)")
        st.markdown("""
        **Принцип работы:**
        - Динамический порог на основе статистического анализа
        - Автоматически подстраивается под характер текста
        - Использует скользящее окно для анализа
        
        **Преимущества:**
        - Универсальность
        - Не требует настройки порогов
        - Адаптируется к разным типам текста
        
        **Недостатки:**
        - Менее предсказуемый
        - Может быть нестабильным на коротких текстах
        """)

    elif section == "⚙️ Настройки":
        st.header("⚙️ Настройки и конфигурация")
        
        st.markdown("""
        ## Общие настройки
        
        ### Размер чанка (chunk_size)
        - **Описание:** Целевой размер чанка в символах/токенах
        - **Диапазон:** 100-3000 символов
        - **Рекомендации:** 500-1000 для большинства задач
        
        ### Перекрытие (chunk_overlap)
        - **Описание:** Количество символов/токенов, которые повторяются между соседними чанками
        - **Диапазон:** 0-200 символов
        - **Рекомендации:** 10-20% от размера чанка
        
        ## Настройки API
        
        ### OpenAI API
        - **API Key:** Ключ для доступа к API OpenAI
        - **Model:** Модель эмбеддингов (text-embedding-ada-002, text-embedding-3-small, text-embedding-3-large)
        - **Timeout:** Время ожидания ответа (10-120 секунд)
        
        ### Fallback система
        При недоступности OpenAI API автоматически используется локальная модель Spacy.
        
        ## Продвинутые настройки
        
        ### Semantic параметры
        - **Similarity Threshold:** Порог схожести для семантического разделения (0.1-1.0)
        - **Window Size:** Размер окна для анализа контекста (2-10)
        - **Max Retries:** Количество повторных попыток при ошибках (1-10)
        """)

    elif section == "📈 Анализ результатов":
        st.header("📈 Анализ и интерпретация результатов")
        
        st.markdown("""
        ## Метрики качества
        
        ### Основные показатели
        
        1. **Количество чанков** - общее количество созданных фрагментов
        2. **Средний размер** - средняя длина чанка в символах/словах
        3. **Стандартное отклонение** - показатель консистентности размеров
        4. **Мин/Макс размеры** - диапазон размеров чанков
        
        ### PSR метрики
        
        #### 🚀 Performance (Производительность)
        - **Время выполнения** - скорость обработки текста
        - **Использование памяти** - потребление RAM
        - **Пропускная способность** - символов в секунду
        
        #### 📈 Scalability (Масштабируемость)
        - **Фактор масштабируемости** - как изменяется время с ростом текста
        - **Эффективность** - отношение производительности к размеру
        - **Корреляция размер-время** - линейность зависимости
        
        #### 🎯 Reliability (Надежность)
        - **Консистентность размеров** - стабильность размеров чанков
        - **Семантическая когерентность** - смысловая связность
        - **Точность границ** - качество определения границ
        
        ## Интерпретация результатов
        
        ### Хорошие показатели:
        - **PSR Score > 0.7** - отличное качество
        - **Низкое стандартное отклонение** - консистентные размеры
        - **Высокая семантическая когерентность** - связный контент
        
        ### Проблемы:
        - **Высокое время выполнения** - медленная обработка
        - **Большая вариация размеров** - непредсказуемые чанки
        - **Низкая точность границ** - разрыв смысловых блоков
        """)

    elif section == "🔧 API документация":
        st.header("🔧 API и расширение функциональности")
        
        st.markdown("""
        ## Структура кода
        
        ### Основные модули
        
        ```python
        # Базовые классы
        from chunkers.base import BaseChunker
        from chunkers.registry import CHUNKERS
        
        # Реализации чанкеров
        from chunkers.basic import CharacterChunker, RecursiveChunker
        from chunkers.semantic import SemanticChunker, AdvancedSemanticChunker
        
        # Анализ
        from analyzer import ChunkingAnalyzer
        from analyzer_advanced import PSRAnalyzer
        ```
        
        ### Создание собственного чанкера
        
        ```python
        from chunkers.base import BaseChunker
        from typing import List, Dict, Any
        
        class MyCustomChunker(BaseChunker):
            def __init__(self, custom_param: int = 100):
                self.custom_param = custom_param
            
            def chunk_text(self, text: str, **kwargs) -> List[str]:
                # Ваша логика чанкования
                chunks = []
                # ... ваш код ...
                return chunks
            
            def get_params(self) -> Dict[str, Any]:
                return {
                    'custom_param': self.custom_param,
                    'method': 'custom'
                }
        
        # Регистрация в системе
        from chunkers.registry import CHUNKERS
        CHUNKERS['my_custom'] = MyCustomChunker
        ```
        
        ### Добавление новых метрик
        
        ```python
        from analyzer import ChunkingAnalyzer
        
        class ExtendedAnalyzer(ChunkingAnalyzer):
            def analyze_chunks(self, chunks: List[str], method_name: str) -> Dict[str, Any]:
                # Базовый анализ
                results = super().analyze_chunks(chunks, method_name)
                
                # Добавляем свои метрики
                results['custom_metric'] = self.calculate_custom_metric(chunks)
                
                return results
            
            def calculate_custom_metric(self, chunks: List[str]) -> float:
                # Ваша логика расчета метрики
                return 0.0
        ```
        
        ## Конфигурация
        
        ### config.py
        - Настройки страницы Streamlit
        - Инициализация session state
        - Глобальные параметры
        
        ### Переменные окружения
        ```bash
        export OPENAI_API_KEY="your-api-key"
        export CHUNKER_DEFAULT_SIZE=1000
        export CHUNKER_DEBUG=true
        ```
        
        ## Развертывание
        
        ### Локальное развертывание
        ```bash
        pip install -r requirements.txt
        streamlit run app.py
        ```
        
        ### Docker
        ```dockerfile
        FROM python:3.9-slim
        COPY . /app
        WORKDIR /app
        RUN pip install -r requirements.txt
        EXPOSE 8501
        CMD ["streamlit", "run", "app.py"]
        ```
        """)
        
        st.code("""
        # Пример использования API
        from chunkers.registry import CHUNKERS
        from analyzer import ChunkingAnalyzer
        
        # Создаем чанкер
        chunker = CHUNKERS['character'](chunk_size=500, chunk_overlap=50)
        
        # Обрабатываем текст
        text = "Ваш длинный текст..."
        chunks = chunker.chunk_text(text)
        
        # Анализируем результаты
        analyzer = ChunkingAnalyzer()
        results = analyzer.analyze_chunks(chunks, 'character')
        
        print(f"Создано {results['chunk_count']} чанков")
        print(f"Средний размер: {results['avg_chunk_size']} символов")
        """, language='python')

    # Дополнительная информация
    st.sidebar.markdown("---")
    st.sidebar.markdown("### 🔗 Полезные ссылки")
    st.sidebar.markdown("- [GitHub репозиторий](https://github.com)")
    st.sidebar.markdown("- [Streamlit документация](https://docs.streamlit.io)")
    st.sidebar.markdown("- [LangChain chunkers](https://langchain.readthedocs.io)")

    st.sidebar.markdown("### 📞 Поддержка")
    st.sidebar.markdown("Если у вас есть вопросы или предложения, создайте issue в GitHub репозитории.")

    # Поиск по документации
    st.sidebar.markdown("---")
    search_query = st.sidebar.text_input("🔍 Поиск по документации")
    if search_query:
        st.sidebar.markdown(f"Ищем: '{search_query}'...")
        # Здесь можно добавить реальный поиск по содержимому 